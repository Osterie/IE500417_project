To run this project you need to have these libraries installed:
- Pandas
- Numpy
- Dash
- Plotly
If you don't have these installed, you can install them using pip:

```
pip install pandas numpy dash plotly
``` 


To run the Dash application, use the following command in your terminal:

```
python app.py
```

run it from the root of the project directory.